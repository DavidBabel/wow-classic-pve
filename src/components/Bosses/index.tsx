import React, { ChangeEvent } from 'react';
import { Raids } from '../../types/database.type';
// TODO clean if not used
interface Props {
  raids: Raids;
}

export function Bosses({ raids }: Props) {
  return '';
}
